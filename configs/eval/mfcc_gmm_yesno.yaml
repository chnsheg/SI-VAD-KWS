experiment_name: mfcc_gmm_yesno
manifest: data/yesno_synth/manifest.jsonl
algorithm: mfcc_gmm
run_dir: runs/mfcc_gmm_yesno

features:
  sample_rate: 16000
  frame_ms: 25.0
  hop_ms: 10.0
  n_mels: 64
  n_mfcc: 64

eval:
  split: test
  tune_split: val
  tune_threshold: true

postprocess:
  threshold: auto
  min_speech_ms: 60.0
  min_silence_ms: 100.0

